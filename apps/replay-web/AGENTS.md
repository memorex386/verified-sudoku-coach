# Replay web guidance

Read the active work package, [trust boundaries](../../docs/architecture/trust-boundaries.md), and
[release runbook](../../docs/runbooks/release.md). The replay is static, synthetic, accessible, and
credential-free. It must not depend on provider packages or imply that recorded behavior is live.
